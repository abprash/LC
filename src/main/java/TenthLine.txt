# Read from the file file.txt and output the tenth line to stdout.
#tail -n will skip those many lines and print from then
#we use the output of that to head to just take line 1 off that
#bash script


tail -n +10 file.txt | head -1  